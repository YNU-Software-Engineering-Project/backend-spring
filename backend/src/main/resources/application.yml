server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://db:3306/testdb?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: password

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: none

logging:
  level:
    org.hibernate.sql: debug

jwt:
  issuer: "SpeedSpark"
  secretKey: "F2C45E466AD5CD862BF16FDA25B64"